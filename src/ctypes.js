define(function(){
    return {
        int     : Int32Array,
        short   : Int16Array,
        float   : Float32Array,
        double  : Float64Array,
        string  : Uint16Array,
        time    : Float64Array,
        temporal: Float64Array,
        integer : Int32Array,
        numeric : Float32Array,
        nominal : Uint16Array
    }
});
